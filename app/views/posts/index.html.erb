<!-- new post link -->
<div class="new-post-link">
  <%= link_to "New Post", new_post_path %>
</div>

<div class="">
  <div class="posts-home-img">
    <p>img goes here</p>
  </div>
  <div class="p-4 posts-list">
    <% @posts.each do |post| %>
    <div class="card p-4 m-4 rounded" style="width: 50%">
      <div class="card-header post-title">
        <h5 class="text-center"><b><%= post.title %></b></h5>
      </div>
      <div class="card-body d-flex align-items-center post-description">
        <div class="d-block">
          <% if post.image.attached? %>
              <%= image_tag (post.thumbnail) %>
            <% else %>
              <p>No image attached to the post</p>
          <% end %>
        </div>
        <div class="p-4">
          <span><%= post.description %></span>
        </div>
      </div>
      <div class="">
        <p>Posted <%= time_tag post.created_at, "data-local": "time-ago" %></p>
      </div>
      <div class="post-show">
        <%= link_to "Show", post_path(post), class: "btn btn-success" %>
      </div>
    </div>
    <% end %>
  </div>
  <div class="row">
    <p>Another row</p>
  </div>
</div>
