<%# comments#show %>

<div class="">
  <% @comments.each do |comment| %>
    <%= comment.post %>
    <%= comment.id %>
    <%= comment.content %>
  <% end %>
</div>

<%= link_to "comments", post_comments_path %>
