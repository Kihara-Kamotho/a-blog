<h2>Edit Comment</h2>

<%= form_with(model: [@post, @comment], local: true) do |form| %>
  <div class="form-group m-4">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="m-4">
      <%= form.submit %>
  </div>

<% end %>
