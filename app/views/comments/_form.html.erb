
<%= form_with(model: [post, comment], local: true) do |form| %>
  <div class="form-group">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <%= form.submit %>
<% end %>
